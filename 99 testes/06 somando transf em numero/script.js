let soma = 0
let entrada = prompt("Digite um número para somar. Para parar, digite PARAR.")
num = parseInt(entrada)
soma += num
entrada = prompt("Digite um número para somar. Para parar, digite PARAR.")
num = parseInt(entrada)
soma += num
alert(`a soma é: ${soma}`)

